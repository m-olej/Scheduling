{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cargo build p2_solver",
            "type": "shell",
            "command": "cargo",
            "args": ["build", "--bin", "p2_solver"],
            "group": { "kind": "build", "isDefault": true }
        }
    ]
}